load("@aspect_rules_ts//ts:defs.bzl", "ts_project")

ts_project(
    name = "bar",
    srcs = [
        "bar.ts",
    ],
    args = ["--listFiles"],
    declaration = True,
    resolve_json_module = True,
    deps = [
        "//examples/resolve_json_module/foo:ts",
    ],
)
